# Jupyter

# Install

```
brew install anaconda
vi ~/.zshrc
chmod ~/.zshrc
ls -lo ~/.zshrc
chmod 777 ~/.zshrc
ls /usr/local/anaconda3/bin
export PATH="/usr/local/anaconda3/bin:$PATH"
mkdir jupyter
cd jupyter/
jupyter notebook
```